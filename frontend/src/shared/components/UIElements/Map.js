import React from "react";

import "./Map.css";

const Map = (props) => {
  //TODO: implement map later
  return <div className={`map ${props.className}`} style={props.style}></div>;
};

export default Map;
